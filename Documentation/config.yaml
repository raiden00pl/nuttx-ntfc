config:                           # common, global configuration options
  cwd: ''                         # path of work directory. eg: /home/work
  build_dir: ''                   # (optional) path to build directory when NTFC is used for building
  build_env:                      # (optional) env vars passed to cmake configure/build for all cores
    CC: ''                        # example: gcc-14
    CXX: ''                       # example: g++-14
  kv:                             # (optional) Kconfig overrides applied to generated .config before build
    CONFIG_EXAMPLE: "y"           # values: y/m/n, numbers, hex, strings
    CONFIG_NAME: "value"
  timeout: 120                    # timeout per test case. Defaults to 120
  timeout_session: 2400           # timeout per session. Defaults to 2400
  loops: 1                        # specify the number of times to run each testcase. Defaults to 1
  heartbeat:                      # (optional) heartbeat monitoring configuration
    enabled: False                # enable heartbeat monitoring. Defaults to False
    interval: 60                  # heartbeat check interval in seconds (min: 30). Defaults to 60
    threshold: 3                  # consecutive failures before declaring busyloop. Defaults to 3
  recovery:                       # device recovery configuration
    max_retries: 3                # maximum number of reboot attempts before giving up. Defaults to 3
    base_delay: 2.0               # initial delay in seconds between retries (doubles each attempt, capped at 60). Defaults to 2.0

product:                          # many products can be supported in tests (product == product0)

  name: "product"                 # product name
  platform: "amp"                 # Platform type: "amp" (default) or "smp"
  ignored_cores: ["dsp"]          # (optional) core names skipped in get_core_info. Defaults to ["dsp"]

  debug:                          # (optional) advanced debug features, see Documentation/debug/index.rst
    coredump:                     # coredump collection on test failure
      enable: false               # enable coredump collection (default: false)
      type: 'auto'                # backend: 'auto' (default), 'gdb', 'fastboot',
                                  #   'ymodem', 'local_file' or 'syslog'
      limit: 5                    # max coredumps collected per session (default: 5)
    gdb:                          # GDB-based debugging
      enable: false               # enable the GDB backend (default: false)
      force_panic: false          # force a panic on the device before coredump (default: false)
      target: ''                  # 'target remote' address, eg 'localhost:1234' or '/tmp/gdb.socket'
      gdb_path: 'gdb'             # GDB executable, eg 'gdb-multiarch' for cross targets
      plugin: false               # load the NTFC in-GDB Python plugin (default: false)
      setup_cmds: []              # raw GDB commands sent after attach, eg
                                  #   - 'ntfcautobp bp up_assert ntfcautogcore -d $RESULT_DIR gcore'
      gcore_cmd: 'gcore'          # command used to write corefiles
      nx_plugin: ''               # path to NuttX pynuttx gdbinit.py
      osabi: ''                   # 'set osabi <value>' sent when non-empty
      auto_breakpoints: false     # plant standard crash/poweroff breakpoints
      mmleak: false               # add ntfcmmleak to the poweroff breakpoint
      attach: false               # PID-attach to sim process (excludes target)
      use_sudo: false             # prefix attach-mode GDB with sudo
    local_file:                   # host-filesystem coredump backend
      core_dir: ''                # watched host directory (required to enable)
      pattern: '*.core'           # corefile glob pattern
    syslog:                       # serial syslog coredump backend
      enable: false               # decode base64+lzf coredump from output
    fastboot:                     # fastboot coredump transport
      dev_sn: ''                  # fastboot device serial number (required to enable this backend)
      mem_addr: '0x40000000'      # memory dump start address
      mem_size: '0x08000000'      # memory dump size
    ymodem:                       # ymodem coredump transport
      serial_port: ''             # host serial port, eg '/dev/ttyUSB0' (required to enable)
      baud_rate: 921600           # serial baud rate for the transfer
      sbrb_path: ''               # path to the sbrb.py ymodem receiver script (required to enable)
                                  # amp: Each core has its own device instance
                                  # smp: All cores share the same device, automatic core switching
  cores:                          # Cores configuration
    core0:                        # Main core (core 0)
      name: ''                    # Core name
      device: ''                  # Choose a device: sim, qemu
      exec_path: ''               # Path of emulator execution for QEMU targets eg: `qemu-system-x86_64`
                                  # Path of serial port for serial targets, eg: `/dev/ttyACM0`
                                  # Empty for simulator.
      exec_args: ''               # Args for emulator execution for QEMU tragets.
                                  # Serial port configuration for serial targets, eg '9600,n,8,1'
                                  # Empty for simulator.
      exec_cwd: ''                # (optional) working directory for the spawned sim/qemu process.
                                  # Kernel-mode hostfs mounts resolve relative to this directory.
                                  # Defaults to the core build directory for kernel-mode builds.
      boot_timeout: 5             # (optional) seconds to wait for the first shell prompt. Defaults to 5
      app_bindir: ''              # (optional) directory with kernel-mode application binaries.
                                  # Defaults to the bin/ directory next to the NuttX ELF
                                  # for kernel-mode builds.

                                  # NTFC can use pre-build image or build it from defconfig
                                  # the behavior will depend on the parameters specified in config.
      conf_path: ''               # (optional) configuration path for this core firmware
      elf_path: ''                # (optional) NuttX ELF path for this core firmware
      defconfig: ''               # (optional) path to build config if NTFC is used for building
      flash: ''                   # (optional) flash command if NTFC is used for flashing
      reboot: ''                  # (optional) System command to reset DUT, eg 'st-flash reset'
      poweroff: ''                # (optional) System command to power off DUT
      flash_only: false           # (optional) build/flash this core but skip boot checks,
                                  # requirement validation, logs, and test execution on it
      line_buffered: false        # (optional) send each command in one transport write.
                                  # Keep false for the default byte-wise behavior.
                                  # Enable for sim or a serial transport with flow control.
      dcmake:                     # (optional) Defines passed to CMake build
        DEFINE1: "VALUE1"
        DEFINE2: "VALUE2"
      build_env:                  # (optional) env vars passed to cmake for this core (overrides config.build_env)
        CC: ''                    # example: gcc-14
        CXX: ''                   # example: g++-14
      kv:                         # (optional) per-core Kconfig overrides (higher priority than config.kv)
        CONFIG_CORE_ONLY: "y"
        CONFIG_EXAMPLE: "n"       # overrides same key from config.kv


    core1:                        # (optional) Aux core 1  AMP test cases)
      name: ''
      device: ''
      conf_path: ''
      elf_path: ''

    core2:                        # (optional) Aux core 2
      name: ''
      device: ''
      conf_path: ''
      elf_path: ''

    core3:                        # (optional) Aux core 2
      name: ''
      device: ''
      conf_path: ''
      elf_path: ''

    core4:                        # (optional) Aux core 2
      name: ''
      device: ''
      conf_path: ''
      elf_path: ''

    core5:                        # (optional) Aux core 2
      name: ''
      device: ''
      conf_path: ''
      elf_path: ''

product1:                         # (optional) next product configuration
  name: "product1" 
  cores:           
    core0:         
      name: ''
      device: ''
      conf_path: ''
      elf_path: ''

    core1:         
      name: ''
      device: ''
      conf_path: ''
      elf_path: ''

    core2:         
      name: ''
      device: ''
      conf_path: ''
      elf_path: ''

    core3:         
      name: ''
      device: ''
      conf_path: ''
      elf_path: ''

    core4:         
      name: ''
      device: ''
      conf_path: ''
      elf_path: ''

    core5:         
      name: ''
      device: ''
      conf_path: ''
      elf_path: ''
